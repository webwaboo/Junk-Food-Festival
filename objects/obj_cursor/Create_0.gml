// Position de départ (ligne 0)
ligne_index = 0;

// Distance entre les lignes (en pixels)
ligne_spacing = 32;

/*no need since we use the grid manager
// Position fixe en X, ajuster selon ta room
x = 0;
// Initialiser la position en Y
y = 0 + (ligne_spacing * ligne_index);
*/

// Find the grid manager once
grid_manager = instance_find(obj_grid_manager, 0);

